This repo, in the docs directory, contains the jekyll implementation of the rosdabbler website.
This is intended for use with github.io

To run locally:
```
cd docs
# first time only
bundle install
# each time
bundle exec jekyll serve
```

Open browser to localhost:4000 to view.
